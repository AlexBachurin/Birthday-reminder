import styled from "styled-components";

export const Wrapper = styled.div`
	.title {
		font-size: 20px;
		text-align: center;
	}
	.register-btn {
		border: none;
		outline: none;
		background: transparent;
		color: blue;
		cursor: pointer;
	}
`;
